const initialState = {
    sheets: [],
    active: null
}

export const sheetReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
